apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rielu
  namespace: argocd
spec:
  project: websites
  source:
    repoURL: REPLACE_WITH_YOUR_REPO_URL
    targetRevision: HEAD
    path: k8s/sites/site-a
  destination:
    server: https://kubernetes.default.svc
    namespace: rielu
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rielugen
  namespace: argocd
spec:
  project: websites
  source:
    repoURL: REPLACE_WITH_YOUR_REPO_URL
    targetRevision: HEAD
    path: k8s/sites/rielugen
  destination:
    server: https://kubernetes.default.svc
    namespace: rielugen
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rielulinks
  namespace: argocd
spec:
  project: websites
  source:
    repoURL: REPLACE_WITH_YOUR_REPO_URL
    targetRevision: HEAD
    path: k8s/sites/rielulinks
  destination:
    server: https://kubernetes.default.svc
    namespace: rielulinks
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
