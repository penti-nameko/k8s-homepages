apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-rielulinks-ingress
  namespace: web-rielulinks
  annotations:
    kubernetes.io/ingress.class: "public"
spec:
  ingressClassName: public
  rules:
    - host: rielulinks.uniproject.jp
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-rielulinks
                port:
                  number: 80
