apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: web-rielulinks
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: index.html
    files:
      - index.html
